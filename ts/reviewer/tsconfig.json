{
    "extends": "../tsconfig.json",
    "include": ["*"],
    // "references": [{ "path": "../lib" }],
    "compilerOptions": {
        // css-browser-selector fails if our output bundle is strict
        "alwaysStrict": false,
        "composite": false
    }
}
