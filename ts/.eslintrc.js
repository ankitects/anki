module.exports = {
    parserOptions: {
        /* This will resolve to each source directories' tsconfig,
         * e.g. for ts/editor it will resolve to ts/editor/tsconfig.json */
        project: ["./tsconfig.json"],
    }
}
