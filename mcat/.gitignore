# Large generated synthetic decks — reproducible via make_synthetic_deck.py,
# so we don't commit them (the 50k deck is ~24 MB).
fixtures/synthetic_*.anki2
fixtures/synthetic_*.anki2-shm
fixtures/synthetic_*.anki2-wal
fixtures/synthetic_*.media/

# Throwaway demo base folder produced by seed_demo.py (reproducible).
fixtures/demo_base/
