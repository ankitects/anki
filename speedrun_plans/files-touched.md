# Upstream files touched + merge difficulty

> **Wednesday hand-in (spec §7a item 4 / PRD §6.5).** Reflects the integrated FR-3
> engine change as built and tested (7 Rust unit tests + 2 Python tests passing).

## Summary

- Total upstream files **modified** (merge-relevant): **5** (each additive; 1–2 lines or one new block)
- New fork-only files/dirs added (no merge-conflict risk): **6** groups
- Auto-generated files (regenerated by the build, never hand-edited, not merge-relevant): proto `*_pb2.py`/`.pyi`, `_backend_generated.py`, `ts/lib/generated/backend.ts`, `OUT_DIR/backend.rs`
- **Overall future-merge difficulty: LOW** — the engine change is purely additive (a new `concepts` module + a new proto service); no edits to the FSRS scheduler core, queue-building, or any hot upstream path.

**Merge-difficulty scale:** **Low** = new file or additive block, unlikely to conflict;
**Medium** = edits inside an existing function/section upstream changes occasionally;
**High** = edits to a hot upstream file (scheduler core, queue building) likely to conflict.

## New fork-only files (Low — additive, cannot conflict)

| File / dir                      | What it is                                                                                                                                                         |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `proto/anki/concepts.proto`     | New `ConceptsService` (+ empty `BackendConceptsService`) with `ConceptAwareQueue` and `ConceptMastery` RPCs and their messages. Auto-discovered by the proto glob. |
| `rslib/src/concepts/mod.rs`     | NTR engine: tag→concept matching, deterministic `weakness`/`NTR`, `compute_mastery`, `order_by_ntr`, and the set-based collection loader. 5 unit tests.            |
| `rslib/src/concepts/service.rs` | Implements the generated `ConceptsService` trait on `Collection`; read-only queue re-ordering + mastery query. 2 unit tests (incl. read-only/undo assertion).      |
| `pylib/tests/test_concepts.py`  | 1 Python test file, 2 tests: calls both RPCs through the backend on an in-memory collection; asserts undo state untouched.                                         |
| `mcat/`                         | FR-2 taxonomy (`taxonomy.json`), `coverage.py`, fixtures, README. Fork data; no upstream overlap.                                                                  |
| `qt/aqt/mcat/`                  | FR-5/6 deterministic Memory score (`memory_score.py`), Qt panel (`panel.py`), `integration.py` bridge, tests.                                                      |

## Existing upstream files modified (merge-relevant)

| File                              | Change type | What changed                                                                                                                   | Merge difficulty | Notes                                                                                           |
| --------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------ | ---------------- | ----------------------------------------------------------------------------------------------- |
| `rslib/src/lib.rs`                | modified    | Added one line: `pub mod concepts;`                                                                                            | **Low**          | Additive module declaration in an alphabetical list.                                            |
| `rslib/proto/src/lib.rs`          | modified    | Added one line: `protobuf!(concepts, "concepts");`                                                                             | **Low**          | The one manual registration step for a new proto file.                                          |
| `qt/aqt/main.py`                  | modified    | Added a `QAction` to the Tools menu in `setupMenus` + a small `_on_mcat_memory_score` handler. Programmatic (no `.ui` change). | **Low–Medium**   | `setupMenus` is occasionally edited upstream; the block is self-contained and easy to re-apply. |
| `qt/installer/app/pyproject.toml` | modified    | `project_name` rebranded to "MCAT Study (Anki fork)" + a comment.                                                              | **Low**          | Display string only; does not touch `bundle`/`formal_name`/app key.                             |
| `README.md`                       | modified    | Replaced with fork README (exam, AGPL credit, build instructions).                                                             | **Low**          | Fork-identity doc; expected to diverge from upstream.                                           |

## Notes on keeping merges cheap

- The change was kept **additive**: a brand-new Rust module and a brand-new proto **service**, rather than modifying the existing scheduler/queue code. The two edits to existing Rust files (`lib.rs`, `proto/src/lib.rs`) are single additive lines.
- Service indices, Python snake_case methods, and TS clients are all **generated** from the proto descriptors at build time, so no hand-maintained dispatch table was touched — nothing to re-merge there.
- No FSRS interval logic was modified; the queue RPC only re-orders an already-due/new set and is read-only, so it cannot corrupt collections or break undo.
