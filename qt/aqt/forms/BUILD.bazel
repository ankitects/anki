load("@rules_python//python:defs.bzl", "py_binary")
load("compile.bzl", "compile_all")

compile_all(
    srcs = glob(["*.ui"]),
    group = "forms",
    visibility = [
        "//qt/aqt:__pkg__",
    ],
)

py_binary(
    name = "build_ui",
    srcs = ["build_ui.py"],
    legacy_create_init = False,
    deps = ["@pyqt5//:pkg"],
)
