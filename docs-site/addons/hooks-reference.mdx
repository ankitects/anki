---
title: "Hooks Reference"
description: "A generated reference of all hooks and filters available in Anki for add-on development."
---

This page lists all available hooks in Anki. It is generated from [`pylib/tools/genhooks.py`](https://github.com/ankitects/anki/blob/main/pylib/tools/genhooks.py) and [`qt/tools/genhooks_gui.py`](https://github.com/ankitects/anki/blob/main/qt/tools/genhooks_gui.py).This page lists all available hooks in Anki. It is generated from [`pylib/tools/genhooks.py`](https://github.com/ankitects/anki/blob/main/pylib/tools/genhooks.py) and [`qt/tools/genhooks_gui.py`](https://github.com/ankitects/anki/blob/main/qt/tools/genhooks_gui.py).

See [Hooks & Filters](/addons/hooks-and-filters) for usage instructionsSee [Hooks & Filters](/addons/hooks-and-filters) for usage instructions.

## Backend Hooks (`anki.hooks`)

{/* <<<cog
  from tools.mintlify_hooks import render_hooks
  cog.out(render_hooks("pylib/tools/genhooks.py"))
  >>> */}

### `card_odue_was_invalid`

No arguments.

## Backend Hooks (`anki.hooks`)

{/* <<<cog
  from tools.mintlify_hooks import render_hooks
  cog.out(render_hooks("pylib/tools/genhooks.py"))
  >>> */}

### `card_odue_was_invalid`

No arguments.

No docstringdocstring.

---

### `schema_will_change`

---

### `schema_will_change`

**Args:** \
**Returns:** 

No docstring.No docstring.

---

### `notes_will_be_deleted`

No docstring.

---

### `note_will_be_added`

---

### `notes_will_be_deleted`

**Args:** , 

No docstring.

---

### `note_will_be_added`

Allows modifying a note before it's added to the collection.

This hook may be called both when users use the Add screen, and when add-ons like AnkiConnect add notes. It is not called when importing. If you wish to alter the Add screen, use gui\_hooks.add\_cards\_will\_add\_note instead.

---

### `media_files_did_export`

Allows modifying a note before it's added to the collection.

This hook may be called both when users use the Add screen, and when add-ons like AnkiConnect add notes. It is not called when importing. If you wish to alter the Add screen, use gui\_hooks.add\_cards\_will\_add\_note instead.

---

### `media_files_did_export`

Only used by legacy .apkg exporter. Will be deprecated in the futureOnly used by legacy .apkg exporter. Will be deprecated in the future.

---

### `legacy_export_progresslegacy_export_progress`

**Args:** 

Temporary hook used in transition to new import/export codTemporary hook used in transition to new import/export code.

---

### `exporters_list_createdexporters_list_created`

**Args:** 

No docstringNo docstring.

---

### `media_file_filtermedia_file_filter`

Allows manipulating the file path that media will be read fromllows manipulating the file path that media will be read from

---

### `field_filter`

---

### `field_filter`

**Args:** , , , \
**Returns:** 

Allows you to define custom `{{filters:..}}llows you to define custom {{filters:..}}`

Your add-on can check filter\_name to decide whether it should modify field\_text or not before returning it.Your add-on can check filter\_name to decide whether it should modify field\_text or not before returning it.

---

### `note_will_flush`

**Args:** 

---

### `note_will_flush`

**Args:** 

Allow to change a note before it is added/updated in the database.llow to change a note before it is added/updated in the database.

---

### `card_will_flush`

---

### `card_will_flush`

Allow to change a card before it is added/updated in the database.

---

### `card_did_render`

Allow to change a card before it is added/updated in the database.

---

### `card_did_render`

Can modify the resulting text after rendering completes.

---

### `importing_importers`

Can modify the resulting text after rendering completes.

---

### `importing_importers`

Allows updating the list of importers. The resulting list is not saved and should be changed each time the filter is called.

NOTE: Updates to the import/export code are expected in the coming months, and this hook may be replaced with another solution at that time. Tracked on [https://github.com/ankitects/anki/issues/1018](https://github.com/ankitects/anki/issues/1018)

---

### `deck_added` _(Obsolete)_

Allows updating the list of importers. The resulting list is not saved and should be changed each time the filter is called.

NOTE: Updates to the import/export code are expected in the coming months, and this hook may be replaced with another solution at that time. Tracked on [https://github.com/ankitects/anki/issues/1018](https://github.com/ankitects/anki/issues/1018)

---

### `deck_added` _(Obsolete)_

<Warning>
  Obsolete, do not use.
</Warning>

---

### `note_type_added` _(Obsolete)_

<Warning>
  Obsolete, do not use.
</Warning>

---

### `note_type_added` _(Obsolete)_

**Args:** 

<Warning>
  Obsolete, do not use.
</Warning>

---

### `sync_stage_did_change` _(Obsolete)_

**Args:** 

<Warning>
  Obsolete, do not use.
</Warning>

---

### `sync_progress_did_change` _(Obsolete)_

**Args:** 

<Warning>
  Obsolete, do not use.
</Warning>

{/* <<<end>>> */}

## GUI Hooks (`aqt.gui_hooks`)

{/* <<<cog
  from tools.mintlify_hooks import render_hooks
  cog.out(render_hooks("qt/tools/genhooks_gui.py"))
  >>> */}

### `overview_did_refresh`

**Args:** 

<Warning>
  Obsolete, do not use.
</Warning>

---

### `sync_stage_did_change` _(Obsolete)_

**Args:** 

<Warning>
  Obsolete, do not use.
</Warning>

---

### `sync_progress_did_change` _(Obsolete)_

**Args:** 

<Warning>
  Obsolete, do not use.
</Warning>

{/* <<<end>>> */}

## GUI Hooks (`aqt.gui_hooks`)

{/* <<<cog
  from tools.mintlify_hooks import render_hooks
  cog.out(render_hooks("qt/tools/genhooks_gui.py"))
  >>> */}

### `overview_did_refresh`

**Args:** 

Allow to update the overview window. E.g. add the deck name in the title.llow to update the overview window. E.g. add the deck name in the title.

---

### `overview_will_render_content`

---

### `overview_will_render_content`

**Args:** , 

Used to modify HTML content sections in the overview bodyUsed to modify HTML content sections in the overview body

'content' contains the sections of HTML content the overview body will be updated with.

When modifying the content of a particular section, please make sure your changes only perform the minimum required edits to make your add-on work. You should avoid overwriting or interfering with existing data as much as possible, instead opting to append your own changes, e.g.:

```python
def on_overview_will_render_content(overview, content):
    content.table += "\n<div>my html</div>"
```

---

'content' contains the sections of HTML content the overview body will be updated with.

When modifying the content of a particular section, please make sure your changes only perform the minimum required edits to make your add-on work. You should avoid overwriting or interfering with existing data as much as possible, instead opting to append your own changes, e.g.:

```python
def on_overview_will_render_content(overview, content):
    content.table += "\n<div>my html</div>"
```

---

### `overview_will_render_bottomwill_render_bottom`

Allows adding buttons to the Overview bottom bar.llows adding buttons to the Overview bottom bar.

Append a list of strings to 'links' argument to add new buttonsppend a list of strings to 'links' argument to add new buttons.

- The first value is the shortcut to appear in the tooltip.
- The second value is the url to be triggered.
- The third value is the text of the new button.

Extend the callable 'link\_handler' to handle new urls. This callable accepts one argument: the triggered url. Make a check of the triggered url, call any functions related to that trigger, and return the new link\_handler.

- The first value is the shortcut to appear in the tooltip.
- The second value is the url to be triggered.
- The third value is the text of the new button.

Extend the callable 'link\_handler' to handle new urls. This callable accepts one argument: the triggered url. Make a check of the triggered url, call any functions related to that trigger, and return the new link\_handler.

Example:Example:

```python
links.append(['H', 'hello', 'Click me!'])
def custom_link_handler(url):
    if url == 'hello':
        print('Hello World!')
    return link_handler(url=url)
return custom_link_handler
```

---

### `reviewer_did_show_question`

```python
links.append(['H', 'hello', 'Click me!'])
def custom_link_handler(url):
    if url == 'hello':
        print('Hello World!')
    return link_handler(url=url)
return custom_link_handler
```

---

### `reviewer_did_show_question`

No docstring.

No docstring.

---

### `reviewer_will_compare_answerreviewer_will_compare_answer`

Modify expected answer and provided answer before comparingModify expected answer and provided answer before comparing

expected\_provided\_tuple is a tuple composed of:expected\_provided\_tuple is a tuple composed of:

- expected answer
- provided answer type\_pattern is the detail of the type tag on the card

Return a tuple composed of:

- modified expected answer
- modified provided answer

---

### `reviewer_will_render_compared_answer`

Modify the output of default compare answer feature

- expected answer
- provided answer type\_pattern is the detail of the type tag on the card

Return a tuple composed of:

- modified expected answer
- modified provided answer

---

### `reviewer_will_render_compared_answer`

**Args:** , , , \
**Returns:** 

Modify the output of default compare answer feature

output is the result of default compare answer function initial\_expected is the expected answer from the card initial\_provided is the answer provided during review type\_pattern is the detail of the type tag on the cardoutput is the result of default compare answer function initial\_expected is the expected answer from the card initial\_provided is the answer provided during review type\_pattern is the detail of the type tag on the card

Return a string comparing expected and provided answersReturn a string comparing expected and provided answers

---

### `reviewer_did_showdid_show_answer`

**Args:** 

No docstring.No docstring.

---

### `reviewer_will_init_answer_buttons`

---

### `reviewer_will_init_answer_buttons`

**Args:** , , \
**Returns:** 

Used to modify list of answer buttonsUsed to modify list of answer buttons

buttons\_tuple is a tuple of buttons, with each button represented by a tuple containing an int for the button's ease and a string for the button's label.

Return a tuple of the form ((int, str), ...), e.g.:

```python
((1, "Label1"), (2, "Label2"), ...)
```

buttons\_tuple is a tuple of buttons, with each button represented by a tuple containing an int for the button's ease and a string for the button's label.

Return a tuple of the form ((int, str), ...), e.g.:

```python
((1, "Label1"), (2, "Label2"), ...)
```

Note: import \_ from anki.lang to support translation, using, e.g.,Note: import \_ from anki.lang to support translation, using, e.g.,

```python
((1, _("Label1")), ...)
```

---

### `reviewer_will_answer_card`

```python
((1, _("Label1")), ...)
```

---

### `reviewer_will_answer_card`

Used to modify the ease at which a card is rated or to bypass rating the card completely.

ease\_tuple is a tuple consisting of a boolean expressing whether the reviewer should continue with rating the card, and an integer expressing the ease at which the card should be rated.

Used to modify the ease at which a card is rated or to bypass rating the card completely.

ease\_tuple is a tuple consisting of a boolean expressing whether the reviewer should continue with rating the card, and an integer expressing the ease at which the card should be rated.

If your code just needs to be notified of the card rating event, you should use the reviewer\_did\_answer\_card hook instead.If your code just needs to be notified of the card rating event, you should use the reviewer\_did\_answer\_card hook instead.

---

### `reviewer_did_answer_card`

---

### `reviewer_did_answer_card`

**Args:** , , 

No docstring.No docstring.

---

### `reviewer_will_show_context_menu`

---

### `reviewer_will_show_context_menu`

No docstring.No docstring.

---

### `reviewer_will_end`

No arguments.

Called before Anki transitions from the review screen to another screen.

---

### `reviewer_will_play_question_sounds`

---

### `reviewer_will_end`

No arguments.

Called before Anki transitions from the review screen to another screen.

---

### `reviewer_will_play_question_sounds`

Called before showing the question/front sideCalled before showing the question/front side.

`tags` can be used to inspect and manipulate the sounds that will be played (if any)`tags` can be used to inspect and manipulate the sounds that will be played (if any).

This won't be called when the user manually plays sounds using `Replay Audio`This won't be called when the user manually plays sounds using `Replay Audio`.

Note that this hook is called even when the `Automatically play audio` option is unchecked; This is so as to allow playing custom sounds regardless of that option.

---

### `reviewer_will_play_answer_sounds`

Note that this hook is called even when the `Automatically play audio` option is unchecked; This is so as to allow playing custom sounds regardless of that option.

---

### `reviewer_will_play_answer_sounds`

Called before showing the answer/back sideCalled before showing the answer/back side.

`tags` can be used to inspect and manipulate the sounds that will be played (if any).

This won't be called when the user manually plays sounds using `Replay Audio`.

`tags` can be used to inspect and manipulate the sounds that will be played (if any).

This won't be called when the user manually plays sounds using `Replay Audio`.

Note that this hook is called even when the `Automatically play audio` option is unchecked; This is so as to allow playing custom sounds regardless of that option.Note that this hook is called even when the `Automatically play audio` option is unchecked; This is so as to allow playing custom sounds regardless of that option.

---

### `reviewer_will_replay_recording`

Used to inspect and modify a recording recorded by "Record Own Voice" before replaying.

---

### `reviewer_will_suspend_note`

**Args:** 

No docstring.

---

### `reviewer_will_suspend_card`

---

### `reviewer_will_replay_recording`

**Args:** \
**Returns:** 

Used to inspect and modify a recording recorded by "Record Own Voice" before replaying.

---

### `reviewer_will_suspend_note`

**Args:** 

No docstring.

---

### `reviewer_will_suspend_card`

No docstringNo docstring.

---

### `reviewer_will_bury_note`

**Args:** 

No docstring.

---

### `reviewer_will_bury_card`

---

### `reviewer_will_bury_note`

**Args:** 

No docstring.

---

### `reviewer_will_bury_card`

No docstringNo docstring.

---

### `audio_did_pause_or_unpause`

**Args:** 

Called when the audio is paused or unpaused. This hook is triggered by the action in Anki's More menu or the related key binding. The webview is provided in case you wish to use this hook with web-based audio.

---

### `audio_did_seek_relative`

---

### `audio_did_pause_or_unpause`

**Args:** 

Called when the audio is paused or unpaused. This hook is triggered by the action in Anki's More menu or the related key binding. The webview is provided in case you wish to use this hook with web-based audio.

---

### `audio_did_seek_relative`

Called when the audio is sought forward (positive seek) or backwards (negative seek). This hook is triggered by the action in Anki's More menu or the related key binding. The webview is provided in case you wish to use this hook with web-based audio.

---

### `debug_console_will_show`

Called when the audio is sought forward (positive seek) or backwards (negative seek). This hook is triggered by the action in Anki's More menu or the related key binding. The webview is provided in case you wish to use this hook with web-based audio.

---

### `debug_console_will_show`

Allows editing the debug window. E.g. setting a default code, or previous code.

---

### `debug_console_did_evaluate_python`

Allows editing the debug window. E.g. setting a default code, or previous code.

---

### `debug_console_did_evaluate_python`

Allows processing the debug result. E.g. logging queries and result, saving last query to display it later...

---

### `card_layout_will_show`

Allows processing the debug result. E.g. logging queries and result, saving last query to display it later...

---

### `card_layout_will_show`

Allow to change the display of the card layout. After most values are set and before the window is actually shown.

---

### `reviewer_did_init`

Allow to change the display of the card layout. After most values are set and before the window is actually shown.

---

### `reviewer_did_init`

Called after the reviewer is initialized.

---

### `audio_will_replay`

Called after the reviewer is initialized.

---

### `audio_will_replay`

Called when the user uses the 'replay audio' action, but not when they click on a play buttonuser uses the 'replay audio' action, but not when they click on a play button.

---

### `card_will_showcard_will_show`

Can modify card text before review/preview.Can modify card text before review/preview.

---

### `card_review_webview_did_init`

---

### `card_review_webview_did_init`

**Args:** , 

Called when initializing the webview for the review screen, the card layout screen, and the preview screen.Called when initializing the webview for the review screen, the card layout screen, and the preview screen.

---

### `deck_browser_did_render`

**Args:** 

Allow to update the deck browser window. E.g. change its title.

---

### `deck_browser_will_render_content`

---

### `deck_browser_did_render`

**Args:** 

Allow to update the deck browser window. E.g. change its title.

---

### `deck_browser_will_render_content`

Used to modify HTML content sections in the deck browser body

'content' contains the sections of HTML content the deck browser body will be updated with.

Used to modify HTML content sections in the deck browser body

'content' contains the sections of HTML content the deck browser body will be updated with.

When modifying the content of a particular section, please make sure your changes only perform the minimum required edits to make your add-on work. You should avoid overwriting or interfering with existing data as much as possible, instead opting to append your own changes, e.g.:When modifying the content of a particular section, please make sure your changes only perform the minimum required edits to make your add-on work. You should avoid overwriting or interfering with existing data as much as possible, instead opting to append your own changes, e.g.:

```python
def on_deck_browser_will_render_content(deck_browser, content):
    content.stats += "\n<div>my html</div>"
```

---

### `deck_conf_did_setup_ui_form`

**Args:** 

```python
def on_deck_browser_will_render_content(deck_browser, content):
    content.stats += "\n<div>my html</div>"
```

---

### `deck_conf_did_setup_ui_form`

**Args:** 

Allows modifying or adding widgets in the deck options UI formllows modifying or adding widgets in the deck options UI form

---

### `deck_conf_will_show`

**Args:** 

Allows modifying the deck options dialog before it is shown

---

### `deck_conf_did_load_config`

---

### `deck_conf_will_show`

**Args:** 

Allows modifying the deck options dialog before it is shown

---

### `deck_conf_did_load_config`

Called once widget state has been set from deck config

---

### `deck_conf_will_save_config`

Called once widget state has been set from deck config

---

### `deck_conf_will_save_config`

Called before widget state is saved to configlled before widget state is saved to config

---

### `deck_conf_did_add_configdeck_conf_did_add_config`

Allows modification of a newly created config groupllows modification of a newly created config group

This hook is called after the config group was created, but before initializing the widget stateThis hook is called after the config group was created, but before initializing the widget state.

`deck_conf` will point to the old config group, `new_conf_id` will point to the newly created config group.

Config groups are created as clones of the current one.

---

### `deck_conf_will_remove_config`

`deck_conf` will point to the old config group, `new_conf_id` will point to the newly created config group.

Config groups are created as clones of the current one.

---

### `deck_conf_will_remove_config`

Called before current config group is removed

---

### `deck_conf_will_rename_config`

Called before current config group is removed

---

### `deck_conf_will_rename_config`

Called before config group is renamedCalled before config group is renamed

---

### `deck_options_did_load`

**Args:** 

Can be used to inject extra options into the config screen.

See the example add-ons at: [https://github.com/ankitects/anki-addons/tree/main/demos/deckoptions\_svelte](https://github.com/ankitects/anki-addons/tree/main/demos/deckoptions_svelte) [https://github.com/ankitects/anki-addons/tree/main/demos/deckoptions\_raw\_html](https://github.com/ankitects/anki-addons/tree/main/demos/deckoptions_raw_html)

---

### `filtered_deck_dialog_did_load_deck`

---

### `deck_options_did_load`

**Args:** 

Can be used to inject extra options into the config screen.

See the example add-ons at: [https://github.com/ankitects/anki-addons/tree/main/demos/deckoptions\_svelte](https://github.com/ankitects/anki-addons/tree/main/demos/deckoptions_svelte) [https://github.com/ankitects/anki-addons/tree/main/demos/deckoptions\_raw\_html](https://github.com/ankitects/anki-addons/tree/main/demos/deckoptions_raw_html)

---

### `filtered_deck_dialog_did_load_deck`

Allows updating widget state once the filtered deck config is loaded

---

### `filtered_deck_dialog_will_add_or_update_deck`

Allows updating widget state once the filtered deck config is loaded

---

### `filtered_deck_dialog_will_add_or_update_deck`

Allows modifying the filtered deck config object before it is writtefiltered deck config object before it is written

---

### `filtered_deck_dialog_did_add_or_update_deckfiltered_deck_dialog_did_add_or_update_deck`

Allows performing changes after a filtered deck has been added or updatedllows performing changes after a filtered deck has been added or updated

---

### `default_search`

---

### `default_search`

**Args:** , \
**Returns:** 

Change the default search when the card browser is opened with card `c`.Change the default search when the card browser is opened with card `c`.

---

### `browser_will_show`

**Args:** 

No docstring.

---

### `browser_menus_did_init`

---

### `browser_will_show`

**Args:** 

No docstring.

---

### `browser_menus_did_init`

No docstringNo docstring.

---

### `browser_will_show_context_menu`

No docstring.

---

### `browser_sidebar_will_show_context_menu`

---

### `browser_will_show_context_menu`

**Args:** , 

No docstring.

---

### `browser_sidebar_will_show_context_menu`

No docstring.

---

### `browser_header_will_show_context_menu`

No docstring.

---

### `browser_header_will_show_context_menu`

No docstring.No docstring.

---

### `browser_did_change_rowbrowser_did_change_row`

**Args:** 

No docstring.No docstring.

---

### `browser_will_build_tree`

Used to add or replace items in the browser sidebar tree

---

### `browser_will_build_tree`

**Args:** , , , \
**Returns:** 

Used to add or replace items in the browser sidebar tree

'tree' is the root SidebarItem that all other items are added to.'tree' is the root SidebarItem that all other items are added to.

'stage' is an enum describing the different construction stages of the sidebar tree at which you can interject your changes. The different values can be inspected by looking at aqt.browser.SidebarStage.'stage' is an enum describing the different construction stages of the sidebar tree at which you can interject your changes. The different values can be inspected by looking at aqt.browser.SidebarStage.

If you want Anki to proceed with the construction of the tree stage in question after your have performed your changes or additions, return the 'handled' boolean unchanged.If you want Anki to proceed with the construction of the tree stage in question after your have performed your changes or additions, return the 'handled' boolean unchanged.

On the other hand, if you want to prevent Anki from adding its own items at a particular construction stage (e.g. in case your add-on implements its own version of that particular stage), return 'True'.

If you return 'True' at SidebarStage.ROOT, the sidebar will not be populated by any of the other construction stages. For any other stage the tree construction will just continue as usual.

On the other hand, if you want to prevent Anki from adding its own items at a particular construction stage (e.g. in case your add-on implements its own version of that particular stage), return 'True'.

If you return 'True' at SidebarStage.ROOT, the sidebar will not be populated by any of the other construction stages. For any other stage the tree construction will just continue as usual.

For example, if your code wishes to replace the tag tree, you could do:For example, if your code wishes to replace the tag tree, you could do:

```python
def on_browser_will_build_tree(handled, root, stage, browser):
    if stage != SidebarStage.TAGS:
        # not at tag tree building stage, pass on
        return handled

    # your tag tree construction code
    # root.addChild(...)

    # your code handled tag tree construction, no need for Anki
    # or other add-ons to build the tag tree
    return True
```

---

### `browser_will_search`

**Args:** 

```python
def on_browser_will_build_tree(handled, root, stage, browser):
    if stage != SidebarStage.TAGS:
        # not at tag tree building stage, pass on
        return handled

    # your tag tree construction code
    # root.addChild(...)

    # your code handled tag tree construction, no need for Anki
    # or other add-ons to build the tag tree
    return True
```

---

### `browser_will_search`

**Args:** 

Allows you to modify the search text, or perform your own search.llows you to modify the search text, or perform your own search.

You can modify context.search to change the text that is sent to the searching backend.You can modify context.search to change the text that is sent to the searching backend.

If you need to pass metadata to the browser\_did\_search hook, you can do it with context.addon\_metadata. For example, to trigger filtering based on a new custom filter.If you need to pass metadata to the browser\_did\_search hook, you can do it with context.addon\_metadata. For example, to trigger filtering based on a new custom filter.

If you set context.ids to a list of ids, the regular search will not be performed, and the provided ids will be used instead.

Your add-on should check if context.ids is not None, and return without making changes if it has been set.

If you set context.ids to a list of ids, the regular search will not be performed, and the provided ids will be used instead.

Your add-on should check if context.ids is not None, and return without making changes if it has been set.

In versions of Anki lower than 2.1.45 the field to check is context.card\_ids rather than context.idsIn versions of Anki lower than 2.1.45 the field to check is context.card\_ids rather than context.ids

---

### `browser_did_search`

**Args:** 

Allows you to modify the list of returned card ids from a search.

---

### `browser_did_fetch_row`

---

### `browser_did_search`

**Args:** 

Allows you to modify the list of returned card ids from a search.

---

### `browser_did_fetch_row`

Allows you to add or modify content to a row in the browser.

You can mutate the row object to change what is displayed. Any columns the backend did not recognize will be returned as an empty string, and can be replaced with custom content.

Allows you to add or modify content to a row in the browser.

You can mutate the row object to change what is displayed. Any columns the backend did not recognize will be returned as an empty string, and can be replaced with custom content.

You can retrieve metadata passed from browser\_will\_search with context.addon\_metadata (for example to trigger post-processing filtering).You can retrieve metadata passed from browser\_will\_search with context.addon\_metadata (for example to trigger post-processing filtering).

Columns is a list of string values identifying what each column in the row represents.Columns is a list of string values identifying what each column in the row represents.

---

### `browser_did_fetch_columns`

**Args:** 

---

### `browser_did_fetch_columns`

**Args:** 

Allows you to add custom columns to the browser.llows you to add custom columns to the browser.

columns is a dictionary of data objects. You can add an entry with a custom column to describe how it should be displayed in the browser or modify existing entries.columns is a dictionary of data objects. You can add an entry with a custom column to describe how it should be displayed in the browser or modify existing entries.

Every column in the dictionary will be toggleable by the userEvery column in the dictionary will be toggleable by the user.

---

### `previewer_did_initpreviewer_did_init`

**Args:** 

Called after the previewer is initialized.Called after the previewer is initialized.

---

### `previewer_will_redraw_after_show_both_sides_toggled`

---

### `previewer_will_redraw_after_show_both_sides_toggled`

**Args:** , , , 

Called when the checkbox \<show both sides\> is toggled by the user.Called when the checkbox \<show both sides\> is toggled by the user.

---

### `state_will_change`

No docstring.

---

### `state_did_change`

---

### `state_will_change`

**Args:** , 

No docstring.

---

### `state_did_change`

No docstring.

---

### `state_shortcuts_will_change`

No docstring.

---

### `state_shortcuts_will_change`

No docstring.No docstring.

---

### `state_did_undo`

---

### `state_did_undo`

Called after backend undoes a changeCalled after backend undoes a change.

---

### `state_did_reset`

No arguments.

Legacy 'reset' hook. Called by mw.reset() and CollectionOp() to redraw the UI.

New code should use `operation_did_execute` instead.

---

### `operation_did_execute`

---

### `state_did_reset`

No arguments.

Legacy 'reset' hook. Called by mw.reset() and CollectionOp() to redraw the UI.

New code should use `operation_did_execute` instead.

---

### `operation_did_execute`

Called after an operation completes. Changes can be inspected to determine whether the UI needs updatingCalled after an operation completes. Changes can be inspected to determine whether the UI needs updating.

This will also be called when the legacy mw.reset() is usedThis will also be called when the legacy mw.reset() is used.

---

### `focus_did_change`

---

### `focus_did_change`

Called each time the focus changes. Can be used to defer updates from `operation_did_execute` until a window is brought to the front.

---

### `backend_will_block`

Called each time the focus changes. Can be used to defer updates from `operation_did_execute` until a window is brought to the front.

---

### `backend_will_block`

No arguments.No arguments.

Called before one or more DB tasks are run in the backgroundCalled before one or more DB tasks are run in the background.

Subscribers can use this to set a flag to avoid DB queries until the operation completes, as doing so will freeze the UI until the long-running operation completes.

---

### `backend_did_block`

Subscribers can use this to set a flag to avoid DB queries until the operation completes, as doing so will freeze the UI until the long-running operation completes.

---

### `backend_did_block`

No arguments.No arguments.

Called after one or more DB tasks finish running in the background. Called regardless of the success of individual operations, and only called when there are no outstanding opsCalled after one or more DB tasks finish running in the background. Called regardless of the success of individual operations, and only called when there are no outstanding ops.

---

### `theme_did_change`

---

### `theme_did_change`

No argumNo arguments.

Called after night mode is toggled.

---

### `body_classes_need_update`

Called after night mode is toggled.

---

### `body_classes_need_update`

No arguments.No arguments.

Called when a setting involving a webview body class is toggledCalled when a setting involving a webview body class is toggled.

---

### `webview_did_receive_js_message`

Used to handle pycmd() messages sent from Javascript.

---

### `webview_did_receive_js_message`

**Args:** , , \
**Returns:** 

Used to handle pycmd() messages sent from Javascript.

Message is the string passed to pycmd().Message is the string passed to pycmd().

For messages you don't want to handle, return 'handled' unchanged.For messages you don't want to handle, return 'handled' unchanged.

If you handle a message and don't want it passed to the original bridge command handler, return (True, None)If you handle a message and don't want it passed to the original bridge command handler, return (True, None).

If you want to pass a value to pycmd's result callback, you can return it with (True, some\_value).

Context is the instance that was passed to set\_bridge\_command(). It can be inspected to check which screen this hook is firing in, and to get a reference to the screen. For example, if your code wishes to function only in the review screen, you could do:

```python
if not isinstance(context, aqt.reviewer.Reviewer):
    # not reviewer, pass on message
    return handled

if message == "my-mark-action":
    # our message, call onMark() on the reviewer instance
    context.onMark()
    # and don't pass message to other handlers
    return (True, None)
else:
    # some other command, pass it on
    return handled
```

---

### `webview_will_set_content`

Used to modify web content before it is rendered.

If you want to pass a value to pycmd's result callback, you can return it with (True, some\_value).

Context is the instance that was passed to set\_bridge\_command(). It can be inspected to check which screen this hook is firing in, and to get a reference to the screen. For example, if your code wishes to function only in the review screen, you could do:

```python
if not isinstance(context, aqt.reviewer.Reviewer):
    # not reviewer, pass on message
    return handled

if message == "my-mark-action":
    # our message, call onMark() on the reviewer instance
    context.onMark()
    # and don't pass message to other handlers
    return (True, None)
else:
    # some other command, pass it on
    return handled
```

---

### `webview_will_set_content`

**Args:** , 

Used to modify web content before it is rendered.

Web\_content contains the HTML, JS, and CSS the web view will be populated with.Web\_content contains the HTML, JS, and CSS the web view will be populated with.

Context is the instance that was passed to stdHtml(). It can be inspected to check which screen this hook is firing in, and to get a reference to the screen. For example, if your code wishes to function only in the review screen, you could do:Context is the instance that was passed to stdHtml(). It can be inspected to check which screen this hook is firing in, and to get a reference to the screen. For example, if your code wishes to function only in the review screen, you could do:

```python
def on_webview_will_set_content(web_content: WebContent, context):

    if not isinstance(context, aqt.reviewer.Reviewer):
        # not reviewer, do not modify content
        return

    # reviewer, perform changes to content

    context: aqt.reviewer.Reviewer

    addon_package = mw.addonManager.addonFromModule(__name__)

    web_content.css.append(
        f"/_addons/{addon_package}/web/my-addon.css")
    web_content.js.append(
        f"/_addons/{addon_package}/web/my-addon.js")

    web_content.head += "<script>console.log('my-addon')</script>"
    web_content.body += "<div id='my-addon'></div>"
```

```python
def on_webview_will_set_content(web_content: WebContent, context):

    if not isinstance(context, aqt.reviewer.Reviewer):
        # not reviewer, do not modify content
        return

    # reviewer, perform changes to content

    context: aqt.reviewer.Reviewer

    addon_package = mw.addonManager.addonFromModule(__name__)

    web_content.css.append(
        f"/_addons/{addon_package}/web/my-addon.css")
    web_content.js.append(
        f"/_addons/{addon_package}/web/my-addon.js")

    web_content.head += "<script>console.log('my-addon')</script>"
    web_content.body += "<div id='my-addon'></div>"
```

---

### `webview_will_show_context_menuwebview_will_show_context_menu`

No docstring.No docstring.

---

### `webview_did_inject_style_into_page`

**Args:** 

---

### `webview_did_inject_style_into_page`

**Args:** 

Called after standard styling is injected into an external html file, such as when loading the new graphs. You can use this hook to mutate the DOM before the page is revealed.Called after standard styling is injected into an external html file, such as when loading the new graphs. You can use this hook to mutate the DOM before the page is revealed.

For example:For example:

```python
def mytest(webview: AnkiWebView):
    if webview.kind != AnkiWebViewKind.DECK_STATS:
        return
    webview.eval(
        """
        div = document.createElement("div");
        div.innerHTML = 'hello';
        document.body.appendChild(div);
        """
    )

gui_hooks.webview_did_inject_style_into_page.append(mytest)
```

```python
def mytest(webview: AnkiWebView):
    if webview.kind != AnkiWebViewKind.DECK_STATS:
        return
    webview.eval(
        """
        div = document.createElement("div");
        div.innerHTML = 'hello';
        document.body.appendChild(div);
        """
    )

gui_hooks.webview_did_inject_style_into_page.append(mytest)
```

---

### `main_window_did_initmain_window_did_init`

No arguments.No arguments.

Executed after the main window is fully initializedExecuted after the main window is fully initialized

A sample use case for this hook would be to delay actions until Anki objects like the profile or collection are fully initialized. In contrast to `profile_did_open`, this hook will only fire once per Anki session and is thus suitable for single-shot subscribersA sample use case for this hook would be to delay actions until Anki objects like the profile or collection are fully initialized. In contrast to `profile_did_open`, this hook will only fire once per Anki session and is thus suitable for single-shot subscribers.

---

### `main_window_should_requiremain_window_should_require_reset`

Executed before the main window will require a resetExecuted before the main window will require a reset

This hook can be used to change the behavior of the main window, when other dialogs, like the AddCards or Browser, require a reset from the main window. If you decide to use this hook, make you sure you check the reason for the reset. Some reasons require more attention than others, and skipping important ones might put the main window into an invalid state (e.g. display a deleted note)This hook can be used to change the behavior of the main window, when other dialogs, like the AddCards or Browser, require a reset from the main window. If you decide to use this hook, make you sure you check the reason for the reset. Some reasons require more attention than others, and skipping important ones might put the main window into an invalid state (e.g. display a deleted note).

---

### `backup_did_complebackup_did_complete`

No arguments.No arguments.

No docstring.No docstring.

---

### `profile_did_open`

No arguments.

Executed whenever a user profile has been opened

---

### `profile_did_open`

No arguments.

Executed whenever a user profile has been opened

Please note that this hook will also be called on profile switches, so if you are looking to simply delay an add-on action in a single-shot manner, `main_window_did_init` is likely the more suitable choice.Please note that this hook will also be called on profile switches, so if you are looking to simply delay an add-on action in a single-shot manner, `main_window_did_init` is likely the more suitable choice.

---

### `profile_will_close`

No arguments.

No docstring.

---

### `collection_will_temporarily_close`

---

### `profile_will_close`

No arguments.

No docstring.

---

### `collection_will_temporarily_close`

Called before one-way syncs and colpkg imports/exports.

---

### `collection_did_temporarily_close`

Called before one-way syncs and colpkg imports/exports.

---

### `collection_did_temporarily_close`

Called after one-way syncs and colpkg imports/exports.

---

### `collection_did_load`

Called after one-way syncs and colpkg imports/exports.

---

### `collection_did_load`

No docstring.

---

### `undo_state_did_change`

No docstring.

---

### `undo_state_did_change`

No docstring.

---

### `style_did_init`

No docstring.

---

### `style_did_init`

No docstringNo docstring.

---

### `top_toolbar_did_init_links`

---

### `top_toolbar_did_init_links`

Used to modify or add links in the top toolbar of Anki's main windowUsed to modify or add links in the top toolbar of Anki's main window

'links' is a list of HTML link elements. Add-ons can generate their own links by using aqt.toolbar.Toolbar.create\_link. Links created in that way can then be appended to the link list, e.g.'links' is a list of HTML link elements. Add-ons can generate their own links by using aqt.toolbar.Toolbar.create\_link. Links created in that way can then be appended to the link list, e.g.:

```python
def on_top_toolbar_did_init_links(links, toolbar):
    my_link = toolbar.create_link(...)
    links.append(my_link)def on_top_toolbar_did_init_links(links, toolbar):
    my_link = toolbar.create_link(...)
    links.append(my_link)
```

---

### `top_toolbar_will_set_left_traytop_toolbar_will_set_left_tray_content`

Used to add custom add-on components to the _left_ area of Anki's main window toolbarUsed to add custom add-on components to the _left_ area of Anki's main window toolbar

'content' is a list of HTML strings added by add-ons which you can append your own components or elements to. To equip your components with logic and styling please see `webview_will_set_content` and `webview_did_receive_js_message`'content' is a list of HTML strings added by add-ons which you can append your own components or elements to. To equip your components with logic and styling please see `webview_will_set_content` and `webview_did_receive_js_message`.

Please note that Anki's main screen is due to undergo a significant refactor in the future and, as a result, add-ons subscribing to this hook will likely require changes to continue workingPlease note that Anki's main screen is due to undergo a significant refactor in the future and, as a result, add-ons subscribing to this hook will likely require changes to continue working.

---

### `top_toolbar_will_set_right_tray_content`

Used to add custom add-on components to the _right_ area of Anki's main window toolbar

---

### `top_toolbar_will_set_right_tray_content`

**Args:** , 

Used to add custom add-on components to the _right_ area of Anki's main window toolbar

'content' is a list of HTML strings added by add-ons which you can append your own components or elements to. To equip your components with logic and styling please see `webview_will_set_content` and `webview_did_receive_js_message`.'content' is a list of HTML strings added by add-ons which you can append your own components or elements to. To equip your components with logic and styling please see `webview_will_set_content` and `webview_did_receive_js_message`.

Please note that Anki's main screen is due to undergo a significant refactor in the future and, as a result, add-ons subscribing to this hook will likely require changes to continue working.Please note that Anki's main screen is due to undergo a significant refactor in the future and, as a result, add-ons subscribing to this hook will likely require changes to continue working.

---

### `top_toolbar_did_redraw`

**Args:** 

---

### `top_toolbar_did_redraw`

**Args:** 

Executed when the top toolbar is redrawnExecuted when the top toolbar is redrawn

---

### `media_sync_did_progress`

---

### `media_sync_did_progress`

No docstring.

No docstring.

---

### `media_sync_did_start_or_stopedia_sync_did_start_or_stop`

No docstring.No docstring.

---

### `empty_cards_will_show`

**Args:** 

---

### `empty_cards_will_show`

**Args:** 

Allows changing the list of cards to delete.llows changing the list of cards to delete.

---

### `sync_will_start`

---

### `sync_will_start`

No argumentsNo arguments.

No docstring.

---

### `sync_did_finish`

No docstring.

---

### `sync_did_finish`

No argumentsarguments.

Executes after the sync of the collection concluded.

Note that the media sync did not necessarily finish at this point.

---

### `media_check_will_start`

Executes after the sync of the collection concluded.

Note that the media sync did not necessarily finish at this point.

---

### `media_check_will_start`

No argumentsNo arguments.

No docstring.

---

### `media_check_did_finish`

No docstring.

---

### `media_check_did_finish`

Called after Media Check finishes.

`output` provides access to the unused/missing file lists and the text output that will be shown in the Check Media screen.

---

### `day_did_change`

Called after Media Check finishes.

`output` provides access to the unused/missing file lists and the text output that will be shown in the Check Media screen.

---

### `day_did_change`

No argumenNo arguments.

Called when Anki moves to the next day.

---

### `exporter_will_export`

Called when Anki moves to the next day.

---

### `exporter_will_export`

before collection and deck Allows add-ons to be notified of impending deck exports and potentially modify the export options. To perform the export unaltered, please return `export_options` as is, e.g.:

Allows add-ons to be notified of impending deck exports and potentially modify the export options. To perform the export unaltered, please return `export_options` as is, e.g.:

```python
def on_exporter_will_export(export_options: ExportOptions, exporter: Exporter):
    if not isinstance(exporter, ApkgExporter):
        return export_options
    export_options.limit = ...
    return export_options
```

---

### `exporter_did_export`

```python
def on_exporter_will_export(export_options: ExportOptions, exporter: Exporter):
    if not isinstance(exporter, ApkgExporter):
        return export_options
    export_options.limit = ...
    return export_options
```

---

### `exporter_did_export`

Called after collection and deck exports.

---

### `legacy_exporter_will_export`

---

### `legacy_exporter_did_export`

**Args:** 

---

### `exporters_list_did_initialize`

**Args:** 

Called after the list of exporter classes is created.

---

### `exporters_list_did_initialize`

**Args:** 

llows you to register custom exporters and/or replace existing ones by modifying the exporter list.Called after the list of exporter classes is created.

---

### `dialog_manager_did_open_dialog`

---

### `dialog_manager_did_open_dialog`

Executed after aqt.dialogs creates a dialog window

---

### `add_cards_will_show_history_menu`

Executed after aqt.dialogs creates a dialog window

---

### `add_cards_did_init`

---

### `add_cards_did_init`

**Args:** 

No docstring.

---

### `add_cards_did_add_note`

No docstring.

---

### `add_cards_did_add_note`

---

### `add_cards_will_add_note`

**Args:** , \
**Returns:** 

Decides whether the note should be added to the collection or not. It is assumed to come from the addCards window.

---

### `add_cards_will_add_note`

Decides whether the note should be added to the collection or not. It is assumed to come from the addCards window.reason\_to\_already\_reject is the first reason to reject that was found, or None. If your filter wants to reject, it should replace return the reason to reject. Otherwise return the input.

---

### `add_cards_might_add_note`

**Args:** , 

Allows you to provide an optional reason to reject a note. A yes / no dialog will open displaying the problem, to which the user can decide if they would like to add the note anyway.

---

### `add_cards_might_add_note`

llows you to provide an optional reason to reject a note. A yes / no dialog will open displaying the problem, to which the user can decide if they would like to add the note anyway.optional\_problems is a list containing the optional reasons for which you might reject a note. If your add-on wants to add a reason, it should append the reason to the list.

optional\_problems is a list containing the optional reasons for which you might reject a note. If your add-on wants to add a reason, it should append the reason to the listAn example add-on that asks the user for confirmation before adding a card without tags:

An example add-on that asks the user for confirmation before adding a card without tags:

```python
def might_reject_empty_tag(optional_problems, note):
    if not any(note.tags):
        optional_problems.append("Add cards without tags?")
```

---

### `addcards_will_add_history_entry`

Allows changing the history line in the add-card window.

---

### `add_cards_did_change_note_type` _(Deprecated)_

```python
def might_reject_empty_tag(optional_problems, note):
    if not any(note.tags):
        optional_problems.append("Add cards without tags?")
```

---

### `addcards_will_add_history_entry`

Allows changing the history line in the add-card window.

---

### `add_cards_did_change_note_type` _(Deprecated)_

<Warning>
  Deprecated. Use `addcards_did_change_note_type` instead.
</Warning>

Executed after the user selects a new note type when adding cards.

---

### `addcards_did_change_note_type`

<Warning>
  Deprecated. Use `addcards_did_change_note_type` instead.
</Warning>

Executed after the user selects a new note type when adding cards.

---

### `addcards_did_change_note_type`

Executed after the user selects a new note type when adding cards.

---

### `add_cards_did_change_deck`

Executed after the user selects a new note type when adding cards.

---

### `add_cards_did_change_deck`

Executed after the user selects a new different deck when adding cards.

---

### `editor_did_init_left_buttons`

No docstring.

---

### `editor_did_init_buttons`

---

### `editor_did_init_shortcuts`

**Args:** , 

No docstring.

---

### `editor_will_show_context_menu`

**Args:** , 

No docstring.

---

### `editor_did_fire_typing_timer`

**Args:** 

No docstring.

---

### `editor_did_focus_field`

---

### `editor_did_init_shortcuts`

---

### `editor_will_show_context_menu`

---

### `editor_did_fire_typing_timer`

**Args:** 

No docstring.

---

### `editor_did_focus_field`

No docstring.

---

### `editor_did_unfocus_field`

---

### `editor_did_load_note`

**Args:** 

---

### `editor_did_update_tags`

**Args:** 

No docstring.

---

### `editor_will_munge_html`

---

### `editor_did_update_tags`

**Args:** 

---

### `editor_will_munge_html`

Allows manipulating the text that will be saved by the editor

---

### `editor_will_use_font_for_field`

---

### `editor_web_view_did_init`

**Args:** 

---

### `editor_did_init`

**Args:** 

No docstring.

---

### `editor_will_load_note`

---

### `editor_did_init`

**Args:** 

---

### `editor_will_load_note`

Allows changing the javascript commands to load note before executing it and do change in the QT editor.

---

### `editor_did_paste`

Allows changing the javascript commands to load note before executing it and do change in the QT editor.

---

### `editor_did_paste`

Called after some data is pasted by python into an editor field.

---

### `editor_will_process_mime`

Used to modify MIME data stored in the clipboard after a drop or a paste. Called after the user pastes or drag-and-drops something to Anki before Anki processes the dataThe function should return a new or existing QMimeData object.

The function should return a new or existing QMimeData objec"mime" contains the corresponding QMimeData object. "internal" indicates whether the drop or paste is performed between Anki fields. Most likely you want to skip processing if "internal" was set to True. "extended" indicates whether the user requested an extended paste. "drop\_event" indicates whether the event was triggered by a drag-and-drop or by a right-click paste.

"mime" contains the corresponding QMimeData object. "internal" indicates whether the drop or paste is performed between Anki fields. Most likely you want to skip processing if "internal" was set to True. "extended" indicates whether the user requested an extended paste. "drop\_event" indicates whether the event was triggered by a drag-and-drop or by a right-click paste.

---

### `editor_state_did_change`

---

### `editor_mask_editor_did_load_image`

---

### `editor_mask_editor_did_load_image`

Called when the image occlusion mask editor has completed loading an image.

When adding new notes `path_or_nid` will be the path to the image file. When editing existing notes `path_or_nid` will be the note id.

---

### `tag_editor_did_process_key`

Called when the image occlusion mask editor has completed loading an image.

When adding new notes `path_or_nid` will be the path to the image file. When editing existing notes `path_or_nid` will be the note id.

---

### `tag_editor_did_process_key`

No docstring.

---

### `av_player_will_play`

No docstring.

---

### `av_player_did_begin_playing`

---

### `av_player_did_begin_playing`

**Args:** , 

No docstring.

---

### `av_player_did_end_playing`

---

### `av_player_did_end_playing`

**Args:** 

No docstring.

---

### `av_player_will_play_tags`

No docstring.

---

### `av_player_will_play_tags`

Called before playing a card side's sounds.

`tags` can be used to inspect and manipulate the sounds that will be played (if any).

`tags` can be used to inspect and manipulate the sounds that will be played (if any).

`side` can either be "question" or "answer".

`side` can either be "question" or "answer".`context` is the screen where the sounds will be played (e.g., Reviewer, Previewer, and CardLayout).

`context` is the screen where the sounds will be played (e.g., Reviewer, Previewer, and CardLayout).This won't be called when the user manually plays sounds using `Replay Audio`.

This won't be called when the user manually plays sounds using `Replay Audio`te that this hook is called even when the `Automatically play audio` option is unchecked; This is so as to allow playing custom sounds regardless of that option.

Note that this hook is called even when the `Automatically play audio` option is unchecked; This is so as to allow playing custom sounds regardless of that option.

---

### `addon_config_editor_will_display_json`

---

### `addon_config_editor_will_save_json` _(Deprecated)_

**Args:** \
**Returns:** 

<Warning>
  Deprecated. Use `addon_config_editor_will_update_json` instead.
</Warning>

<Warning>
  Deprecated. Use `addon_config_editor_will_update_json` instead.
</Warning>

---

### `addon_config_editor_will_update_json`

**Args:** , \
**Returns:** 

---

### `addons_dialog_will_show`

**Args:** 

Allows changing the add-on dialog before it is shown. E.g. add buttons.

---

### `addons_dialog_did_change_selected_addon`

---

### `addons_dialog_will_show`

**Args:** 

---

### `addons_dialog_did_change_selected_addon`

Allows doing an action when a single add-on is selected.

---

### `addons_dialog_will_delete_addons`

Allows doing an action when a single add-on is selected.

---

### `addons_dialog_will_delete_addons`

Allows doing an action before an add-on is deleted.

---

### `addon_manager_will_install_addon`

Can be used to release DB connections or open files that would prevent an update from succeedCalled before installing or updating an addon.

Can be used to release DB connections or open files that would prevent an update from succeeding.

---

### `addon_manager_did_install_addon`

Called after installing or updating an addonCan be used to restore DB connections or open files after an add-on has been updated.

Can be used to restore DB connections or open files after an add-on has been updated.

---

### `models_advanced_will_show`

---

### `models_did_init_buttons`

**Args:** , \
**Returns:** 

---

### `fields_did_add_field`

**Args:** , 

No docstring.

---

### `fields_did_rename_field`

---

### `fields_did_add_field`

---

### `fields_did_rename_field`

No docstring.

---

### `fields_did_delete_field`

No docstring.

---

### `fields_did_delete_field`

No docstring.

---

### `stats_dialog_will_show`

---

### `stats_dialog_old_will_show`

**Args:** 

---

### `current_note_type_did_change`

**Args:** 

No docstring.

---

### `sidebar_should_refresh_decks`

---

### `current_note_type_did_change`

**Args:** 

---

### `sidebar_should_refresh_decks`

No arguments.

Legacy, do not use.

---

### `sidebar_should_refresh_notetypes`

Legacy, do not use.

---

### `sidebar_should_refresh_notetypes`

No argumentsLegacy, do not use.

---

### `deck_browser_will_show_options_menu`

**Args:** , 

No docstring.

---

### `flag_label_did_change`

---

### `deck_browser_will_show_options_menu`

Used to update the GUI when a new flag label is assigned.

{/* <<<end>>> */}