default_install_hook_types: [pre-push]

repos:
    - repo: local
      hooks:
          - id: ninja-check
            name: ninja check
            entry: ./ninja check
            language: system
            pass_filenames: false
            always_run: true
            stages: [pre-push]
