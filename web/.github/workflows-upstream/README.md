# Upstream desktop workflows

These workflows came from the standalone Anki desktop repository. GitHub does
not execute workflows from this directory.

They are preserved as references because their commands and artifact paths
assume the desktop source is at the repository root. Promote a workflow back
to `/.github/workflows/` only after adapting paths to the `web/` prefix and
reviewing its release permissions and secrets for this repository.
